import numpy as np

row=int(input("enter no. of rows"))
col=int(input("enter no. of columns"))

x=np.random.randint(low=1,high=5,size=(row,col))              #matrix created of random numbers
print(x)                 


np.savetxt('file.out',x)               #matrix saved in file
